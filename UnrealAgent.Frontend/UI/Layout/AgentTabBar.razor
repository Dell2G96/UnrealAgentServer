<!-- 에이전트 탭 띠 — 흰 상단바와 하늘색 대화방 사이에 끼므로 라이트 톤으로 맞췄다. -->
<div class="flex items-center justify-between px-0 bg-kk-chip border-b border-kk-line shrink-0 h-9">
    <div class="flex items-center h-full overflow-x-auto">
        @* 리더 탭 (항상 존재) *@
        <button class="h-full flex items-center gap-2 px-5 text-[11px] font-semibold tracking-wide transition-colors relative bg-kk-bar text-kk-text border-b-2 border-b-kk-send-fg">
            <span class="font-sans font-black text-kk-send-fg text-xs">U</span>
            <span class="whitespace-nowrap">UnrealAgent</span>
        </button>
    </div>
</div>
